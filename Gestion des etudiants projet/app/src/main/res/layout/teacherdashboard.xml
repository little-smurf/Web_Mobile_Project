<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    android:background="#f5f5f5">
    <ImageView
        android:id="@+id/imageLogout"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@android:drawable/ic_lock_power_off"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="1dp"
        android:layout_marginTop="-1dp"
        android:contentDescription="Logout" />
    <TextView
        android:id="@+id/textGroupLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:text="Select Group:"
        android:textColor="#000000"
        android:textSize="20sp" />

    <Spinner
        android:id="@+id/groupSpinner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/textGroupLabel"
        android:layout_marginTop="10dp"
        android:background="@android:drawable/btn_dropdown" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/groupSpinner"
        android:layout_marginTop="20dp">

        <TableLayout
            android:id="@+id/studentsTableLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:stretchColumns="*"
            android:background="#FFFFFF"
            android:padding="8dp">

            <!-- TableRows and TextViews for displaying student information will be added dynamically -->

        </TableLayout>
    </ScrollView>

</RelativeLayout>
